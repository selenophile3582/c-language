#include <stdio.h>

int main()
{
    int n;
    printf("How many numbers do you want to enter\n");
    scanf("%d", &n);

    int arr[n];
for (int i = 0; i < n; i++) 
{
    scanf("%d",&arr[i]);
    
}
for (int i = 0; i < n; i++)
{
    /* code */
}

    return 0;
}
